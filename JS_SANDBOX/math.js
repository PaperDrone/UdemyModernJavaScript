let val;

val = Math.PI;
val = Math.E;
val = Math.round(2.4);
val = Math.ceil(2.4); // Round-up
val = Math.floor(2.9); // Round-down
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8, 2);
val = Math.min(5, 10, 15, 2);
val = Math.max(5, 10, 15, 2);

val = Math.random(); // 0 ~ 1

val = Math.floor(Math.random() * 20 + 1);

console.log(val);